"""Minimal setup for BinaryStars_sim — editable install adds project root to sys.path."""

from setuptools import setup, find_packages

setup(
    name="binarystars-sim",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.10",
)
